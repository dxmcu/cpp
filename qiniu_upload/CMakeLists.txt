cmake_minimum_required(VERSION 3.0)
project(qiniu_upload)

SET(EXECUTABLE_OUTPUT_PATH "${PROJECT_BINARY_DIR}")
SET(LIBRARY_OUTPUT_PATH "${PROJECT_BINARY_DIR}")

ADD_SUBDIRECTORY(upload)
ADD_SUBDIRECTORY(lib)
